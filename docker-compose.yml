version: '3.8'
services:
  mongo:
    image: mongo:4.4.6
    container_name: betterbrands_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=betterbrands_fantasy

volumes:
  mongo-data: